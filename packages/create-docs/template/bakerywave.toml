schemaVersion = 1

# Bakerywave preset defaults.
# Keep this file minimal and add options only when needed.

[site.navigation]
autoNavigationFromDocsFolders = true

[site.navigation.sectionLabels]
guides = "Guides"
reference = "API"

[site.footer]
enabled = true
showLicense = true
showCopyright = true
showGithub = true
showPoweredBy = true

# Add [reference], [i18n], [site.search], and [site.locale] only when required.
